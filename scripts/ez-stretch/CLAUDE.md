# EZ Stretch BSC

Global context (signing, PJSR patterns, release workflow) is in `~/.claude/CLAUDE.md`.

## Project-Specific

- **Scripts**: PhotometricStretch, LuptonRGB, RNC-ColorStretch
- Push directly to main — plugin suite, not critical infrastructure
- **Release tools**: `tools/sign.sh`, `tools/build-packages.sh`, `tools/install-local.sh`
